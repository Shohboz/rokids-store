<%= form_for(@category_product) do |f| %>
  <% if @category_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_product.errors.count, "error") %> prohibited this category_product from being saved:</h2>

      <ul>
      <% @category_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.text_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>  
  <br>
  <br>
  
  <%= link_to 'Submit', '#', class: "btn btn-large red accent-2 waves-effect waves-light", onclick: "$('form').submit()" %>
  <%= f.submit nil, style: "display: none;" %>

  <br>
  <br>
<% end %>
